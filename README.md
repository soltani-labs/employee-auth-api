# employee-auth-api
Spring Boot REST API with JWT authentication and role-based access control with roles USER and ADMIN. Employees are also users in the system. Admin can create, update, delete and list employees. They can also promote a user to administrator. For normal users, login only. The API is built on top of Spring Security, Spring Data JPA and PostgreSQL.
